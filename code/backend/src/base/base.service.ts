/**
 * Base for a generated, transport-neutral Service router.
 *
 * Services expose application operations to Controllers but contain no business
 * behavior. `module:sync` owns their construction and direct delegation code.
 */
export abstract class BaseService {}
