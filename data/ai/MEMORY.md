2026-07-22 10:20 CEST — Executable backend paths use the repository layout rooted at `code/backend`. TypeScript source, local imports, architecture-linter filename rules, and package entry/script references consistently use `.ts`. The architecture linter derives the project root from `import.meta.url`, so `npm run lint:backend` works from both the repository root and `code/backend`.
2026-07-22 10:23 CEST — `.DS_Store` files and the local `.code-graph`, `.smallcode`, and `.memory` directories are ignored repository-wide. These machine-local artifacts should remain available locally but must not appear in Git status or commits.
2026-07-22 10:28 CEST — The repository is an npm-workspace monorepo. The root is the private orchestrator for `code/backend` (`@app/backend`) and `code/frontend/web` (`@app/web`), owns the only `package-lock.json`, and provides `npm run verify` for workspace linting, typechecking, and tests. Runtime and directly imported dependencies remain declared by the workspace that uses them; shared TypeScript tooling is declared at the root.
