2026-07-22 10:20 CEST — Executable backend paths use the repository layout rooted at `code/backend`. TypeScript source, local imports, architecture-linter filename rules, and package entry/script references consistently use `.ts`. The architecture linter derives the project root from `import.meta.url`, so `npm run lint:backend` works from both the repository root and `code/backend`.
